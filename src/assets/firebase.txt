Initial setup
npm run ng build --prod
npm install -g firebase-tools

open command prompt inside project
firebase login
firebase init
firebase deploy


after setup done just we need to use deploy command
